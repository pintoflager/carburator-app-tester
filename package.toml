name = "tester"
version = "0.1.0"
collection = "App"
platforms = ["Server", "Client"]

priority = 20
provides = ["dummy_tester"]
# os_support = []

[recommend]
on_level = "Project" # *Proxy | Group | Project
# count = 1 # How many nodes on the given level // leave empty for all
message = """
Installs tester dummy app on every node to try how executors etc work.
"""
